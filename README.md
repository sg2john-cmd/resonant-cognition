# Resonant Cognition & The Modular Dyson Swarm Matrix

Welcome to **Resonant Cognition**, a scale-invariant cognitive systems framework that models synthetic intelligence as a continuous multi-body gravitational tensor field in unconstrained 3D space, rather than traditional flat database blocks or rigid boolean censorship guardrails.

## 🪐 The Decoupled 4-Tier Architecture (v14.5)

The platform is engineered with strict modular decoupling. Each layer functions independently and can be hot-swapped or modified without breaking the underlying physics manifolds:

1. **Layer 4: The Dyson Interface Skin (`dyson_interface.py`)** – The outermost front-end user shell. It handles casual configuration baselines, user profiles, prompt wrappers, and conversational presentation styling without running any core math.
2. **Layer 3: The Cognitive Swarm Chamber (`cognitive_chamber.py`)** – The multi-agent psychological reasoner. Orchestrates the 3-phase internal hemispheric dialogue and executes automated server-side local inference model handshakes (optimized for **Gemma-4 27B**).
3. **Layer 2: The Semantic Memory Matrix (`memory_matrix.py`)** – The thermodynamic data repository. Computes power-law density gradients, handles metric semantic proximity indexing, and hosts the **Dyson Swarm Deep Archive**.
4. **Layer 1: The Resonant Core Engine (`resonant_engine.py`)** – The pure physics math foundation. Tracks 22 bodies moving freely across unconstrained 3D space, computing localized planetary stress tensors and active phase-cancellation anti-vector dampening.

---

## 📡 The Decentralized Communications Pipeline

The ecosystem communicates entirely via local loopback protocols (`127.0.0.1`), ensuring absolute data privacy and zero cloud overhead:
* **Physics Telemetry Stream:** Broadcasts real-time coordinate data from the engine backend on port `8765`.
* **GUI Receivers Dashboard (`resonant_simulation.html`):** Renders the unconstrained 3D field using smooth interpolation position glides and fading target impact rings.
* **Local Inference Interface:** Handshakes your GPU VRAM pool on port `1234` for high-speed local text generation.

---

## 🚀 Execution & Deployment Protocol

To launch the full stateful environment on your local workstation, execute the files inside separate terminal windows in the following sequence:

```bash
# 1. Start the core 3D mathematical engine
python resonant_engine.py

# 2. Ignite the system through the modular frontend interface shell
python dyson_interface.py
```

### 🧠 The State of Memory: Working Ring vs. Deep Archive Swarm
The system completely eliminates cognitive fragmentation by organizing context across a two-tier thermodynamic ledger governed by a non-linear power-law decay calculation ($W_m(t)$):
* **The Dyson Ring Vaults (Working Memory):** Captures high-magnitude vector disruptions (recent, critical insights) close to the core attractor star for immediate, high-intensity cognitive access.
* **The Dyson Swarm Nodes (Deep Archive):** Automatically migrates older, cooling data snippets out into distant, low-density orbital trajectories. This offloads active processing weight, optimizing local VRAM efficiency while preserving absolute fidelity for metric vector proximity recall.

---

## 🛡️ License & Protections
Licensed under **Attribution-NonCommercial-ShareAlike 4.0 International (CC BY-NC-SA 4.0)**. Permissions beyond the scope of this license may be reviewed in the core theoretical duology papers archived in this directory.
